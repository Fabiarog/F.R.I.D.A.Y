{
  "name": "friday-core",
  "version": "5.3.1",
  "description": "F.R.I.D.A.Y. Local — assistente desktop leve e privado",
  "scripts": {
    "voice:build": "powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File ./voice/build_voice.ps1",
    "predev": "npm run voice:build",
    "dev": "npx @tauri-apps/cli dev",
    "prebuild": "npm run voice:build",
    "build": "npx @tauri-apps/cli build",
    "test": "node --test tests/*.test.js",
    "tauri": "npx @tauri-apps/cli"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
